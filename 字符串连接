#字符串连接#
#include <stdio.h>
#include <string.h>

int main(void){
	
	char str[100],str1[50];
	fgets(str,sizeof(str),stdin);
	str[strcspn(str,"\n")]='\0';
	fgets(str1,sizeof(str1),stdin);
	str1[strcspn(str1,"\n")]='\0';
	
	strcat(str,str1);
	printf("%s",str);
	
	return 0;
}
